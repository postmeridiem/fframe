import 'package:flutter/material.dart';

class NotImplementedError extends StatelessWidget {
  const NotImplementedError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Not implemented error"),
    );
  }
}
