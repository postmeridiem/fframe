name: froute
description: The Amazing Router.
version: 0.0.1
homepage: fframe.app

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  http: 
  test: any
  build_runner:

# The following section is specific to Flutter.
# === this is configured at the application level when using FlutFrame ===