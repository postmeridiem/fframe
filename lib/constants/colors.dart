import 'package:flutter/material.dart';

// DARK MODE =======
// color that sets the appbar, navrail, contextwidgets and FAB
Color constPrimaryColorDark = Color(0xFF1B020B);
Color constOnPrimaryColorDark = Color(0xffffffff);

// color that sets 'secondary' level control widgets
// (application surfaces and things that are not navrail, contextwidgets, FAB or appbar...)
Color constSecondaryColorDark = Color(0xFF310414);
Color constOnSecondaryColorDark = Color(0xFFEEEEEE);

// color that sets the primary accent color
Color constPrimaryAccentColorDark = Color(0xFF008381);

// color for unselected elements
Color constUnselectedColorDark = Color(0xB2E7E7E7);

// color for dividers and lines
Color constDividerColorDark = Color(0xB21B1B1B);

// color that sets the main background behind everything
Color constBackgroundColorDark = Color(0xFF0E0D0D);
Color constOnBackgroundColorDark = Color(0xFFEEEEEE);
Color constOnBackgroundColorFadedDark = Color(0xFF20010E);

// LIGHT MODE =======
// color that sets the appbar, navrail, contextwidgets and FAB
Color constPrimaryColorLight = Color(0xFF310414);
Color constOnPrimaryColorLight = Color(0xFFFFFFFF);

// color that sets 'secondary' level control widgets
// (application surfaces and things that are not navrail, contextwidgets, FAB or appbar...)
Color constSecondaryColorLight = Color(0xFFFFF8FB);
Color constOnSecondaryColorLight = Color(0xB21D1D1D);

// color that sets the primary accent color
Color constPrimaryAccentColorLight = Color(0xFF008381);

// color for unselected elements
Color constUnselectedColorLight = Color(0xB2474747);

// color for dividers and lines
Color constDividerColorLight = Color(0xB2B6B6B6);

// color that sets the main background behind everything
Color constBackgroundColorLight = Color(0xFFECE2E2);
Color constOnBackgroundColorLight = Color(0xFF1B020B);
Color constOnBackgroundColorFadedLight = Color(0xFFDAC8CF);

// Basic Signal Colors
Color constGreenSuccess = Color(0xFF10743D);
Color constRedFailure = Color(0xFF8B3737);
