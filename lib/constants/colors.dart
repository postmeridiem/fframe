import 'package:flutter/material.dart';

// FONTS
// Main font (changing this needs the font assets to be added to the project)
String mainFontFamily = 'OpenSans';
// Monospace font (changing this needs the font assets to be added to the project)
String monoFontFamily = 'RobotoMono';

// DARK MODE =======
// color that sets the appbar, navrail, contextwidgets and FAB
Color constPrimaryColorDark = Color(0xFF1B020B);
Color constOnPrimaryColorDark = Color(0xffffffff);

// color that sets the primary accent color
Color constPrimaryAccentColorDark = Color(0xFF005554);
Color constOnPrimaryAccentColorDark = Color(0xFF020202);

// color that sets 'secondary' level control widgets
// (application surfaces and things that are not navrail, contextwidgets, FAB or appbar...)
Color constSecondaryColorDark = Color(0xFF310414);
Color constOnSecondaryColorDark = Color(0xffffffff);

// color for unselected elements
Color constUnselectedColorDark = Color(0xB2E7E7E7);

// color that sets the main background behind everything
Color constBackgroundColorDark = Color(0xFF1B020B);
Color constOnBackgroundColorDark = Color(0xffffffff);

// LIGHT MODE =======
// color that sets the appbar and navrail and the color on top
Color constPrimaryColorLight = Color(0xFF1B020B);
Color constOnPrimaryColorLight = Color(0xffffffff);

// color that sets the primary accent color
Color constPrimaryAccentColorLight = Color(0xFF1B020B);
Color constOnPrimaryAccentColorLight = Color(0xffffffff);

// color that sets 'secondary' level control widgets
// (application surfaces and things that are not navrail and appbar...)
Color constSecondaryColorLight = Color(0xFF1B020B);
Color constOnSecondaryColorLight = Color(0xffffffff);

// color for unselected elements
Color constUnselectedColorLight = Color(0xB2E7E7E7);

// color that sets the main background behind everything
Color constBackgroundColorLight = Color(0xFF1B020B);
Color constOnBackgroundColorLight = Color(0xFF1B020B);
